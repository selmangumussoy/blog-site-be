server:
  port: 8080

spring:
  application:
    name: blog-site-be
  datasource:
    url: jdbc:mysql://localhost:3306/asim-nesli
    username: root
    password: hSelMan?!11444
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 20MB

jwt:
  secret:
    key: "BuCokGuvenliNebahatVeUzunBirSecretKeyYavuz0123456Asli"
